/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/stackblitz-starters-vr7qt1",
  output: "export",  // <=== enables static exports
  reactStrictMode: true,
};

module.exports = nextConfig;
